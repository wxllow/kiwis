# Installation

The recommended way to install cocoa is using pip or poetry.

## Latest Release

=== "pip"

    ```bash
    pip install cocoa
    ```

=== "Poetry"

    ```bash
    poetry add cocoa
    ```

## Git

=== "Poetry"

    ```bash
    poetry add git+git://github.com/wxllow/cocoa
    ```
