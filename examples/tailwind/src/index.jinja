{% extends "_base.jinja" %}
{% block content %}
    <div class="text-center">
        <h1 class="font-bold text-5xl">{{ site.name }}</h1>
        <p class="font-semibold italic text-2xl">{{ site.slogan }}</p>
    </div>
{% endblock %}
